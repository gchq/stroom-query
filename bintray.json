{
  "package": {
    "name": "stroom-query",
    "repo": "stroom",
    "subject": "stroom"
  },
  "version": {
    "name": "SET_BY_TRAVIS_BUILD",
    "desc": "SET_BY_TRAVIS_BUILD",
    "released": "SET_BY_TRAVIS_BUILD",
    "vcs_tag": "SET_BY_TRAVIS_BUILD",
    "gpgSign": false
  },
  "files": [
    {
      "includePattern": "stroom-query-api/build/libs/(.*\\.jar)",
      "uploadPattern": "$1"
    },
    {
      "includePattern": "stroom-query-audit/build/libs/(.*\\.jar)",
      "uploadPattern": "$1"
    },
    {
      "includePattern": "stroom-query-common/build/libs/(.*\\.jar)",
      "uploadPattern": "$1"
    },
    {
      "includePattern": "stroom-query-hibernate/build/libs/(.*\\.jar)",
      "uploadPattern": "$1"
    }
  ],
  "publish": true
}
